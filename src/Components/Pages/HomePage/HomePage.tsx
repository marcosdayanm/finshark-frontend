import React from "react";
import Hero from "../../Hero/Hero";

type Props = {};

const HomePage = (props: Props) => {
  return (
    <div>
      <Hero />
    </div>
  );
};

export default HomePage;
